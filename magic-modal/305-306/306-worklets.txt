==================================================================
 #306, taken properly: move Worklets up to satisfy reanimated 4.5.3
==================================================================

reanimated 4.5.3 wants worklets 0.10.x - 0.11.x. Bump both packages to
worklets 0.10.3 (latest 0.10.x) and reanimated 4.5.3, everywhere.

$ pnpm install --no-frozen-lockfile

  root reanimated 4.5.3
  root worklets   0.10.3

$ cd packages/modal && pnpm exec jest

Test Suites: 4 passed, 4 total
Tests:       36 passed, 36 total
Ran all test suites.

The suites go green. Then the repo's own gate rejects it:

$ cd examples/kitchen-sink && pnpm expo-doctor

Running 19 checks on your project...
18/19 checks passed. 1 checks failed. Possible issues detected:

X Check that packages match versions required by installed Expo SDK

  Minor version mismatches
  package                expected  found
  react-native-worklets  0.7.4     0.10.3

1 package out of date.

1 check failed, indicating possible issues with the project.

Worklets is a native module and is NOT on the example app's
expo.install.exclude list (only reanimated and gesture-handler are), so
expo-doctor holds it at whatever Expo SDK 55 ships.

There is no arrangement where 4.5.3 lands without leaving the SDK 55 line:
keep worklets at 0.7.4 and jest dies on the version assert, move worklets
and expo-doctor fails. That is a hand-made SDK move, not a Renovate bump.
